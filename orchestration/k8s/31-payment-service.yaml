apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: saga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: orchestration-payment-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          env:
            - { name: DB_URL, value: jdbc:postgresql://postgres:5432/paymentdb }
            - { name: DB_USER, value: postgres }
            - { name: DB_PASSWORD, value: postgres }
            - { name: ZIPKIN_ENDPOINT, value: http://zipkin:9411/api/v2/spans }
          readinessProbe:
            tcpSocket: { port: 8082 }
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: saga
spec:
  selector:
    app: payment-service
  ports:
    - port: 8082
      targetPort: 8082
